#ifndef SPONGENT_HPP
#define SPONGENT_HPP

unsigned char reverse(unsigned char byte);

unsigned char lCounter(unsigned char byte);

unsigned char getBit(unsigned char X, int i);

void pLayer(unsigned char* state);

void permutation(unsigned char* state);
#endif
